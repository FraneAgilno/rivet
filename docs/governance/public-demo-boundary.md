# Public source boundary

Rivet publishes only framework-owned implementation and synthetic examples. Exclude client code, client protocols, ticket contents, private documentation, credentials, and execution history. Repository access alone does not authorize publishing external data.

Review source provenance and the exact publication tree before release. Live demos require dedicated sandbox accounts and synthetic data. License selection and package publication remain separate from source publication.
