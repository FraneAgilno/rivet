# Normalize tags

Implement normalizeTags(tags) in src/solution.js. Use one Worker owning only src/solution.js. Keep all configuration and public checks unchanged. The fixture has no dependencies. Do not install packages, access a network, or push Git branches.

## Acceptance criteria
- Trim whitespace and lowercase each string tag.
- Remove empty normalized tags and duplicates, preserving first occurrence order.
- Return an empty array for empty input.
- Leave the input array unchanged.
